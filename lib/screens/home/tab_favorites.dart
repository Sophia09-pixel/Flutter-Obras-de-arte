import 'package:flutter/material.dart';

class TabFavorites extends StatelessWidget {
  const TabFavorites({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Obras favoritas', style: TextStyle(fontSize: 20)),
    );
  }
}

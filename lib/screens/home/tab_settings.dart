import 'package:flutter/material.dart';

class TabSettings extends StatelessWidget {
  const TabSettings({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Configurações do app', style: TextStyle(fontSize: 20)),
    );
  }
}
